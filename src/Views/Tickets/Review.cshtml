@using Microsoft.AspNetCore.Localization
@using Microsoft.AspNetCore.Mvc.Localization

@inject IViewLocalizer Localizer
@{
    ViewData["Title"] = Localizer["Review for"] + " " + Model.TicketTitle;
}
@using GoldenTicket.ViewModels.Tickets
@using GoldenTicket.Models
@model TicketReviewViewModel

<form method="post">
	<input asp-for="TicketId" type="hidden">
	<input asp-for="ReviewerRole" type="hidden">

	<label>@Localizer["Title"]</label>
	<input asp-for="TicketTitle">

	<p>
		<label>
			<input asp-for="ReviewOutcome" type="radio" value="Approved" onclick="enableConfirm()" />
			<span>@Localizer["Approve"]</span>
		</label>
		<br />
		<label>
			<input asp-for="ReviewOutcome" type="radio" value="Rejected" onclick="enableConfirm()" />
			<span>@Localizer["Reject"]</span>
		</label>
	</p>

	<button class="btn" onclick="goBack()">@Localizer["Go Back"]</button>

	<button class="btn" type="submit" id="btnConfirm" disabled>@Localizer["Confirm"]</button>
</form>

<script>
	function goBack() {
		window.history.back();
	}
	function enableConfirm() {
		document.getElementById("btnConfirm").disabled = false;
	}
</script>